<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:name="com.chairul.sipp_app.fragment.HomeFragment"
        android:label="Beranda"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/navigation_profile"
        android:name="com.chairul.sipp_app.fragment.ProfileFragment"
        android:label="Beranda"
        tools:layout="@layout/fragment_profile" />

    <fragment
        android:id="@+id/navigation_tentang"
        android:name="com.chairul.sipp_app.fragment.TentangFragment"
        android:label="Beranda"
        tools:layout="@layout/fragment_tentang" />
</navigation>